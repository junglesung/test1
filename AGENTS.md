# 專案規定（給所有 Agent）

本檔為專案固定規範。之後開新的 agent 時請直接遵守，無需使用者再次提醒。

## 專案性質

- 這是**靜態網頁**專案。
- 必須可在**手機、平板、電腦**上直接開啟執行（無需複雜建置流程；以靜態檔案為主）。
- 介面文字**全部使用繁體中文**。
- 必須具備 **RWD**：直式與橫式螢幕都要可正常使用與閱讀。

## Git 工作流程

- 每次改完檔案後，**直接 commit 並 push 到 GitHub 的 `main` branch**。
- **不要**另外開 feature branch、**不要**開 PR（除非使用者明確要求）。
- commit message 由 agent **自動產生**，內容需清楚說明本次變更。

## 溝通語言

- 與使用者對話一律使用**繁體中文**。
- 語音輸入內容也視為繁體中文處理與回覆。
